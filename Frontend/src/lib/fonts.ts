
// We'll use CSS imports instead of Next.js font optimization
// These fonts will be imported in our CSS files

// Define font family constants for consistent usage
export const fontFamilies = {
  inter: 'Inter, sans-serif',
  spaceGrotesk: 'Space Grotesk, sans-serif',
  syne: 'Syne, sans-serif',
};

// CSS variables
export const fontVariables = {
  inter: '--font-inter',
  spaceGrotesk: '--font-space-grotesk',
  syne: '--font-syne',
};
